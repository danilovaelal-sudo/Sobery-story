# Лендинг курса «Собери свою историю»

Статический лендинг (HTML/CSS), mobile-first.

## Локальный просмотр
Откройте `index.html` в браузере.

## Публикация на GitHub Pages
После push в репозиторий включите GitHub Pages: Settings → Pages → Deploy from a branch → `main` / `/root`.
